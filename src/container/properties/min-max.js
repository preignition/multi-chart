export default {
  
  /* 
   * `min` min value for the domain
   */
  min: {
    type: Object,
    notify: true
  },

  /* 
   * `min` min value for the domain
   */
  max: {
    type: Object,
    notify: true
  }

  
};